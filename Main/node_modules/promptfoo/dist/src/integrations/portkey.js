"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.getPrompt = void 0;
const node_fetch_1 = __importDefault(require("node-fetch"));
const tiny_invariant_1 = __importDefault(require("tiny-invariant"));
async function getPrompt(id, variables) {
    (0, tiny_invariant_1.default)(process.env.PORTKEY_API_KEY, 'PORTKEY_API_KEY is required');
    const url = `https://api.portkey.ai/v1/prompts/${id}/render`;
    const response = await (0, node_fetch_1.default)(url, {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json',
            'x-portkey-api-key': process.env.PORTKEY_API_KEY,
        },
        body: JSON.stringify({ variables }),
    });
    if (!response.ok) {
        throw new Error(`HTTP error! status: ${response.status}`);
    }
    const result = (await response.json());
    if (!result.success) {
        throw new Error(`Portkey error! ${JSON.stringify(result)}`);
    }
    return result.data;
}
exports.getPrompt = getPrompt;
//# sourceMappingURL=portkey.js.map